{{#if file.isNew}}
  {{#if isUploading}}
    <div class="progress">
      <div class="bar" style={{progress}}></div>
    </div>
  {{else}}
    <span>loading...</span>
  {{/if}}
{{else}}
  <img class="asset-image" src={{file.imageUrl}}/>
  <span>{{file.fileName}}</span>
{{/if}}
